## 0.1.71

A redirect fix for sites behind a CDN or reverse proxy.

Visiting a dashboard page while signed out (for example, after your session
expired) could redirect you to an internal address your browser can't reach,
instead of back to your real site. Those redirects now always land on your
public site address. No changes to your public site.

Updating applies everything automatically.
