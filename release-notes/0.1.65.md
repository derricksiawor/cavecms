## 0.1.65

A small polish to the update history.

Status labels in Settings → Updates (e.g. "Update rolled back", "Update
succeeded") now center their text when they wrap onto two lines, instead of
sitting ragged against the left edge of the badge.

Updating applies everything automatically.
